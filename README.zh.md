# 📝 Text Toolbox - 综合性文本处理工具箱

一个易于扩展、界面美观的Web文本处理工具箱，支持多种文本处理功能。

## ✨ 特性

- 🎨 **美观的Web界面** - 现代化的渐变设计，支持拖拽上传
- 🔧 **易于扩展** - 插件化架构,轻松添加新功能
- 🚀 **高性能** - 基于FastAPI的异步后端
- 📦 **灵活输入** - 支持文件夹多选和ZIP压缩包
- 🌐 **跨平台** - 基于Web，任何浏览器都能使用

## 🛠️ 当前功能

### 1. Markdown文件合并

将多个以日期命名的Markdown文件按时间顺序合并为一个文件。

**支持的文件名格式：**
- `20250410.md`
- `2025-04-10.md`
- `20250410_notes.md`

**合并规则：**
- 合并后的文件名为年份（如 `2025.md`）
- 一级标题为年份（如 `# 2025`）
- 二级标题为格式化日期（如 `## 2025-03-09`）
- 每个文件的内容跟在对应日期标题后
- 文件之间用分割线 `---` 分隔
- 按日期升序排列

**使用方式：**
1. 上传多个.md文件，或
2. 上传包含.md文件的.zip压缩包

## 🏗️ 项目结构

```
text-toolbox/
├── backend/                 # 后端代码
│   ├── main.py             # FastAPI应用主入口
│   ├── requirements.txt    # Python依赖
│   ├── tools/              # 工具模块目录
│   │   ├── __init__.py
│   │   ├── base.py         # 工具基类
│   │   └── md_merger.py    # Markdown合并工具
│   └── temp/               # 临时文件目录
├── frontend/               # 前端代码
│   └── index.html         # 单页应用
├── docker-compose.yml     # Docker编排文件
├── Dockerfile            # Docker镜像
└── README.md             # 项目说明
```

## 🚀 快速开始

### 方式一：使用 Docker（推荐）

```bash
# 1. 启动服务
docker-compose up -d

# 2. 访问应用
浏览器打开: http://localhost:8080
```

### 方式二：本地运行

#### 1. 安装依赖

```bash
cd backend
pip install -r requirements.txt
```

#### 2. 启动后端

```bash
cd backend
python main.py
```

后端API将在 http://localhost:8000 启动

#### 3. 访问前端

直接在浏览器中打开 `frontend/index.html` 文件，或使用任何Web服务器：

```bash
# 使用Python的简单HTTP服务器
cd frontend
python -m http.server 8080
```

然后访问 http://localhost:8080

## 📚 API文档

启动后端后，访问 http://localhost:8000/docs 查看自动生成的API文档。

### 主要端点

- `GET /` - 获取API信息和工具列表
- `GET /api/tools` - 获取所有可用工具
- `POST /api/tools/md-merger` - 合并多个MD文件
- `POST /api/tools/md-merger/zip` - 从ZIP文件中提取并合并MD文件

## 🔧 添加新功能

本项目采用插件化架构，添加新功能非常简单：

### 1. 创建工具类

在 `backend/tools/` 目录下创建新的工具文件，继承 `BaseTool`：

```python
# backend/tools/your_tool.py
from .base import BaseTool

class YourTool(BaseTool):
    @property
    def name(self) -> str:
        return "你的工具名称"
    
    @property
    def description(self) -> str:
        return "工具描述"
    
    async def process(self, *args, **kwargs):
        # 实现你的处理逻辑
        pass
```

### 2. 注册工具

在 `backend/main.py` 中注册你的工具：

```python
from tools.your_tool import YourTool

TOOLS_REGISTRY = {
    "md_merger": {...},
    "your_tool": {
        "name": "你的工具名称",
        "description": "工具描述",
        "endpoint": "/tools/your-tool",
        "handler": YourTool()
    }
}
```

### 3. 添加API端点

在 `backend/main.py` 中添加对应的API端点：

```python
@app.post("/api/tools/your-tool")
async def your_tool_endpoint(file: UploadFile = File(...)):
    # 处理逻辑
    pass
```

### 4. 更新前端界面

在 `frontend/index.html` 中添加新的工具卡片和交互逻辑。

## 🐳 Docker部署

项目包含完整的Docker配置，支持一键部署：

```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

## 🔍 示例

### 输入文件

```
folder/
├── 20250101.md
├── 20250102.md
└── 20250103.md
```

**20250101.md:**
```markdown
今天是2025年的第一天，新年快乐！
```

**20250102.md:**
```markdown
开始制定新年计划。
```

### 输出文件 (2025.md)

```markdown
# 2025

## 2025-01-01

今天是2025年的第一天，新年快乐！

---

## 2025-01-02

开始制定新年计划。

---
```

## 🛣️ 开发路线图

- [x] Markdown文件合并功能
- [ ] 文本格式转换（MD/TXT/PDF等）
- [ ] 批量文件重命名
- [ ] 文本统计分析
- [ ] 正则表达式批量替换
- [ ] CSV/Excel数据处理
- [ ] 文本去重工具

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📄 许可

MIT License

## 👨‍💻 技术栈

- **后端**: FastAPI (Python 3.8+)
- **前端**: 原生HTML/CSS/JavaScript
- **部署**: Docker + Docker Compose
